require "spec_helper"

describe Account do
  it "has lots of useful properties"
  it "can fetch its account information"
  context "given a date range" do
    it "gets transactions"
    it "only fetches transactions when needed"
  end
end